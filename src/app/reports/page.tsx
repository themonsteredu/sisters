import type { Metadata } from "next";
import { AlertCircle, ArrowDown, ArrowUp, BrainCircuit, CalendarDays, CheckCircle2, Clock3, Download, TrendingUp } from "lucide-react";
import { AppShell } from "@/components/shell/app-shell";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";

export const metadata: Metadata = { title: "학습 리포트" };

const subjects = [
  { name: "영어", minseo: 91, jiwoo: 84, delta: 5, color: "bg-violet-500" },
  { name: "수학", minseo: 84, jiwoo: 79, delta: 2, color: "bg-blue-500" },
  { name: "국어", minseo: 78, jiwoo: 86, delta: -1, color: "bg-rose-400" },
  { name: "과학", minseo: 72, jiwoo: 81, delta: -4, color: "bg-emerald-500" },
  { name: "역사", minseo: 86, jiwoo: 88, delta: 3, color: "bg-amber-500" },
];

export default function ReportsPage() {
  return <AppShell><div className="space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-bold text-violet-600">학습 리포트</p><h1 className="mt-1 text-3xl font-black tracking-tight">아이들의 성장 흐름</h1><p className="mt-2 text-sm text-slate-500">완료율뿐 아니라 정시 수행, 재학습, 과목별 이해도를 함께 봅니다.</p></div><div className="flex gap-2"><Button variant="secondary"><CalendarDays size={17} /> 8월 1주</Button><Button variant="secondary"><Download size={17} /> PDF 내보내기</Button></div></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[{ icon: CheckCircle2, label: "가족 완료율", value: "82%", note: "+6%", tone: "bg-emerald-50 text-emerald-600" }, { icon: Clock3, label: "총 학습시간", value: "18h 56m", note: "+2h 10m", tone: "bg-blue-50 text-blue-600" }, { icon: TrendingUp, label: "테스트 평균", value: "84점", note: "+4점", tone: "bg-violet-50 text-violet-600" }, { icon: AlertCircle, label: "보완 필요", value: "3개", note: "과학 · 국어", tone: "bg-rose-50 text-rose-600" }].map((item) => <Card key={item.label} className="p-5"><div className="flex items-center justify-between"><span className={`grid size-10 place-items-center rounded-xl ${item.tone}`}><item.icon size={20} /></span><Badge className="bg-slate-50 text-slate-500">{item.note}</Badge></div><p className="mt-4 text-xs font-bold text-slate-400">{item.label}</p><strong className="mt-1 block text-2xl font-black">{item.value}</strong></Card>)}</section><div className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><Card className="p-5 md:p-6"><div className="flex items-center justify-between"><div><h2 className="text-lg font-black">과목별 성취</h2><p className="mt-1 text-xs text-slate-400">완료율·테스트·부모 승인 결과의 종합 지표</p></div><div className="flex gap-3 text-xs font-bold"><span className="flex items-center gap-1"><i className="size-2 rounded-full bg-violet-500" />민서</span><span className="flex items-center gap-1"><i className="size-2 rounded-full bg-rose-400" />지우</span></div></div><div className="mt-7 space-y-5">{subjects.map((subject) => <div key={subject.name} className="grid grid-cols-[42px_1fr_42px] items-center gap-3"><strong className="text-sm">{subject.name}</strong><div className="space-y-1.5"><Progress value={subject.minseo} indicatorClassName="bg-violet-500" /><Progress value={subject.jiwoo} indicatorClassName="bg-rose-400" /></div><span className={`flex items-center justify-end text-xs font-bold ${subject.delta >= 0 ? "text-emerald-600" : "text-rose-500"}`}>{subject.delta >= 0 ? <ArrowUp size={13} /> : <ArrowDown size={13} />}{Math.abs(subject.delta)}</span></div>)}</div></Card><Card className="overflow-hidden"><div className="bg-gradient-to-r from-violet-600 to-indigo-700 p-5 text-white"><div className="flex items-center gap-2 text-sm font-bold text-violet-100"><BrainCircuit size={18} /> AI 학습 인사이트</div><h2 className="mt-3 text-xl font-black">이번 주는 이것을 챙겨주세요</h2><p className="mt-2 text-xs leading-5 text-violet-100">분석 결과는 부모 판단을 돕는 참고 정보입니다.</p></div><div className="space-y-3 p-5"><div className="rounded-xl border border-rose-100 bg-rose-50 p-4"><Badge className="bg-white text-rose-600">민서 · 과학</Badge><h3 className="mt-2 text-sm font-black">광합성 물질 이동 순서</h3><p className="mt-1 text-xs leading-5 text-slate-500">서술형에서 핵심어 ‘이산화탄소’와 ‘기공’ 연결이 약해요. 오답 3문항 재시험을 추천합니다.</p></div><div className="rounded-xl border border-amber-100 bg-amber-50 p-4"><Badge className="bg-white text-amber-700">지우 · 국어</Badge><h3 className="mt-2 text-sm font-black">설명문의 구조</h3><p className="mt-1 text-xs leading-5 text-slate-500">강의는 완료했지만 요약 메모가 짧아요. 10분 개념노트 과제를 추가해보세요.</p></div><Button className="w-full">보완 일정 2건 검토</Button></div></Card></div></div></AppShell>;
}
