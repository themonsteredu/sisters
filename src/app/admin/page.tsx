import type { Metadata } from "next";
import Link from "next/link";
import { Activity, AlertTriangle, ArrowRight, Bot, CheckCircle2, Clock3, Database, ShieldCheck, Users } from "lucide-react";
import { AppShell } from "@/components/shell/app-shell";
import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";

export const metadata: Metadata = { title: "운영자 대시보드" };

export default function AdminPage() {
  return <AppShell role="admin"><div className="space-y-6"><div><p className="text-sm font-bold text-violet-600">시스템 운영</p><h1 className="mt-1 text-3xl font-black">무료 베타 운영 현황</h1><p className="mt-2 text-sm text-slate-500">가족, AI 작업, 알림과 데이터 요청을 안전하게 관리합니다.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[{ label: "베타 가족", value: "24", detail: "학생 47명", icon: Users, tone: "bg-violet-50 text-violet-600" }, { label: "오늘 AI 작업", value: "186", detail: "성공 98.4%", icon: Bot, tone: "bg-blue-50 text-blue-600" }, { label: "검수 전환", value: "3", detail: "공급자 실패", icon: AlertTriangle, tone: "bg-amber-50 text-amber-600" }, { label: "보관 데이터", value: "1.8GB", detail: "90일 자동삭제", icon: Database, tone: "bg-emerald-50 text-emerald-600" }].map((item) => <Card key={item.label} className="p-5"><span className={`grid size-10 place-items-center rounded-xl ${item.tone}`}><item.icon size={20} /></span><p className="mt-4 text-xs font-bold text-slate-400">{item.label}</p><strong className="mt-1 block text-2xl font-black">{item.value}</strong><span className="mt-1 block text-xs text-slate-400">{item.detail}</span></Card>)}</div><div className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h2 className="text-lg font-black">시스템 상태</h2><p className="mt-1 text-xs text-slate-400">최근 24시간</p></div><Badge className="bg-emerald-50 text-emerald-700"><Activity size={13} className="mr-1" /> 정상</Badge></div><div className="divide-y divide-slate-100">{[{ name: "Supabase 데이터베이스", status: "연결 대기", note: "데모 모드", icon: Database }, { name: "OpenAI 공급자", status: "키 미등록", note: "관리자 입력 가능", icon: Bot }, { name: "Gemini 대체 공급자", status: "키 미등록", note: "관리자 입력 가능", icon: Bot }, { name: "백그라운드 작업 큐", status: "준비됨", note: "Cron 연결 후 실행", icon: Clock3 }].map((item) => <div key={item.name} className="flex items-center gap-3 p-4"><span className="grid size-10 place-items-center rounded-xl bg-slate-100 text-slate-500"><item.icon size={18} /></span><div className="flex-1"><strong className="text-sm">{item.name}</strong><p className="mt-1 text-xs text-slate-400">{item.note}</p></div><Badge>{item.status}</Badge></div>)}</div></Card><Card className="p-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-violet-100 text-violet-700"><ShieldCheck size={21} /></span><div><h2 className="font-black">출시 준비도</h2><p className="mt-1 text-xs text-slate-400">외부 연동 전 체크리스트</p></div></div><div className="mt-5 space-y-3">{["가족별 RLS 정책", "학생 PIN 잠금", "키 암호화 저장", "AI 부모 승인 경계"].map((item) => <div key={item} className="flex items-center gap-2 text-sm"><CheckCircle2 size={17} className="text-emerald-500" />{item}</div>)}{["Supabase 프로젝트 연결", "PortOne 법정대리인 확인", "알림톡 템플릿 승인", "개인정보 법률 검토"].map((item) => <div key={item} className="flex items-center gap-2 text-sm text-slate-500"><Clock3 size={17} className="text-amber-500" />{item}</div>)}</div><Link href="/admin/ai" className="mt-6 flex items-center justify-between rounded-xl bg-violet-50 p-3 text-sm font-bold text-violet-700">AI 공급자 설정 <ArrowRight size={17} /></Link></Card></div></div></AppShell>;
}
